import News from '@/components/News'
import React from 'react'

interface INoticia {
  params: {
    noticia: string,
  }
}

export default async function page({params}: INoticia) {
  return (
    <></>
  )
}
